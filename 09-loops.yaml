- name: checking loops
  hosts: localhost
  become: yes
  tasks:
    - name: print names
      ansible.builtin.debug:
        msg: "Hello {{item}}"
      loops:
        - ramesh
        - rakesh
        - suresh
        - mukesh