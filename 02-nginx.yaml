- name: install and run nginx
  hosts: web
  tasks:
  - name: install nginx package
    ansible.builtin.package:
      name: nginx
      state: present
  - name: run package
    ansible.builtin.service:
      name: nginx
      state: started